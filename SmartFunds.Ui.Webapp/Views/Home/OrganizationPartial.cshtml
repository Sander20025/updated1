@model Organization

<div class="card">
    <div class="card-header">
        @Model.Name
    </div>
    <div class="card-body">
        <h5 class="card-title">@Model.Type</h5>
        <p class="card-text">@Model.CompanyNumber</p>
    </div>
</div>